[03/04, 10:38 am] Shiv Dutt Jha: import pyodbc

# Connect to SQL Server
conn = pyodbc.connect('DRIVER={SQL Server};SERVER=your_server_name;DATABASE=your_database_name;UID=your_username;PWD=your_password')

# Create a cursor
cursor = conn.cursor()

# Create database
cursor.execute("CREATE DATABASE OnlineLearningPlatform")

# Commit the transaction
conn.commit()

# Close the connection
conn.close()
[03/04, 10:38 am] Shiv Dutt Jha: # Connect to the database
conn = pyodbc.connect('DRIVER={SQL Server};SERVER=your_server_name;DATABASE=OnlineLearningPlatform;UID=your_username;PWD=your_password')
cursor = conn.cursor()

# Create Staging Schema
cursor.execute("CREATE SCHEMA Staging")

# Commit the transaction
conn.commit()

# Close the connection
conn.close()
[03/04, 10:38 am] Shiv Dutt Jha: # Assuming you have Excel files with data, use pandas to read Excel files and insert into staging tables
import pandas as pd

# Read Excel files
courses_df = pd.read_excel('courses.xlsx')
users_df = pd.read_excel('users.xlsx')
enrollments_df = pd.read_excel('enrollments.xlsx')
assessments_df = pd.read_excel('assessments.xlsx')
transactions_df = pd.read_excel('transactions.xlsx')

# Connect to the database
conn = pyodbc.connect('DRIVER={SQL Server};SERVER=your_server_name;DATABASE=OnlineLearningPlatform;UID=your_username;PWD=your_password')

# Insert data into Staging tables
courses_df.to_sql('Staging.Courses', conn, if_exists='replace', index=False)
users_df.to_sql('Staging.Users', conn, if_exists='replace', index=False)
enrollments_df.to_sql('Staging.Enrollments', conn, if_exists='replace', index=False)
assessments_df.to_sql('Staging.Assessments', conn, if_exists='replace', index=False)
transactions_df.to_sql('Staging.Transactions', conn, if_exists='replace', index=False)

# Close the connection
conn.close()
[03/04, 10:39 am] Shiv Dutt Jha: # Connect to the database
conn = pyodbc.connect('DRIVER={SQL Server};SERVER=your_server_name;DATABASE=OnlineLearningPlatform;UID=your_username;PWD=your_password')
cursor = conn.cursor()

# Create ODS Schema
cursor.execute("CREATE SCHEMA ODS")

# Commit the transaction
conn.commit()

# Close the connection
conn.close()
[03/04, 10:39 am] Shiv Dutt Jha: # Connect to the database
conn = pyodbc.connect('DRIVER={SQL Server};SERVER=your_server_name;DATABASE=OnlineLearningPlatform;UID=your_username;PWD=your_password')
cursor = conn.cursor()

# Create tables in ODS
# Example:
cursor.execute("""
CREATE TABLE ODS.Courses (
    CourseID INT PRIMARY KEY,
    Title VARCHAR(255),
    Description VARCHAR(1000),
    Category VARCHAR(100),
    InstructorID INT,
    CONSTRAINT FK_Instructor FOREIGN KEY (InstructorID) REFERENCES ODS.Users(UserID)
)
""")

# Commit the transaction
conn.commit()

# Close the connection
conn.close()
[03/04, 10:39 am] Shiv Dutt Jha: # Connect to the database
conn = pyodbc.connect('DRIVER={SQL Server};SERVER=your_server_name;DATABASE=OnlineLearningPlatform;UID=your_username;PWD=your_password')

# Transform and clean data as needed

# Move data to ODS tables
# Example:
cursor = conn.cursor()
cursor.execute("""
INSERT INTO ODS.Courses (CourseID, Title, Description, Category, InstructorID)
SELECT CourseID, Title, Description, Category, InstructorID
FROM Staging.Courses
""")

# Commit the transaction
conn.commit()

# Close the connection
conn.close()
